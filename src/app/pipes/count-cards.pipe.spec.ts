import { CountCardsPipe } from './count-cards.pipe';

describe('CountCardsPipe', () => {
  it('create an instance', () => {
    const pipe = new CountCardsPipe();
    expect(pipe).toBeTruthy();
  });
});
